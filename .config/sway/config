include /etc/sway/config.d/*

###### Autostart ######
exec_always /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec nm-applet --indicator &
exec sworkstyle &> /tmp/sworkstyle.log

###### Setup confing (1: laptop, 2: desktop) ######
set $setupN 2

###### Variables ######
set $mod Mod4
set $term foot
bindsym $mod+d exec wofi --show drun
set $calendar $term -e khal interactive

###### Wallpaper ######
output * bg wallpaper.jpg fill

###### setup configuration ######
include $HOME/.config/sway/setup_$setupN

###### Theming ######
default_border none 
gaps outer 2
gaps inner 2
smart_gaps on
smart_borders off
default_border pixel 1
client.focused #808080 #808080 #808080 #808080 #808080 
set $gnome-schema org.gnome.desktop.interface
exec_always {
	gsettings set $gnome-schema gtk-theme 'Skeuos-Blue-Dark'
	gsettings set $gnome-schema icon-theme 'Vimix-Doder-dark'
	gsettings set $gnome-schema cursor-theme 'Breeze_Default'
}
# Status Bar
bar {
	swaybar_command waybar
}
# wob 
set $WOBSOCK $XDG_RUNTIME_DIR/wob.sock
exec mkfifo $WOBSOCK && tail -f $WOBSOCK | wob
exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK

###### windows behavior ######
# fullscreen
# for_window [class="^.*"] inhibit_idle fullscreen
# for_window [app_id="^.*"] inhibit_idle fullscreen
for_window [app_id="io.github.celluloid_player.Celluloid"] inhibit_idle fullscreen
for_window [app_id="brave-browser"] inhibit_idle fullscreen
for_window [title="Screensaver"] fullscreen enable global
# floating
for_window [app_id="pavucontrol"] floating enable
for_window [app_id="nm-connection-editor"] floating enable
for_window [app_id="blueberry.py"] floating enable
for_window [title="Sharing Indicator"] border none; floating enable
for_window [app_id="com.nextcloud.desktopclient.nextcloud"] floating enable
for_window [app_id="thunar" title="Progress"] floating enable

###### Key bindings ######
# start a terminal
bindsym $mod+Return exec $term
# kill focused window
bindsym $mod+q kill
floating_modifier $mod normal
bindsym $mod+Shift+r reload
# shutdown, reboot, logout, lock, screensaver
bindsym $mod+ctrl+1 exec shutdown now
bindsym $mod+ctrl+2 exec reboot
bindsym $mod+ctrl+3 exec swaymsg 'exit'
bindsym $mod+ctrl+4 exec 'swaylock -f -c 000000'
bindsym $mod+ctrl+5 exec alacritty -t Screensaver -e $SHELL -c 'sleep .1 && cmatrix'
bindsym $mod+ctrl+6 exec alacritty -t Screensaver -e $SHELL -c 'sleep .1 && cmatrix' & swaylock -f -c 000000B3
bindsym $mod+ctrl+7 exec notify-send "DND ON" && sleep 2 && makoctl set-mode dnd
bindsym $mod+ctrl+8 exec makoctl set-mode default && notify-send "DND OFF"
bindsym $mod+ctrl+9 exec killall -SIGUSR1 waybar
bindsym $mod+ctrl+0 exec systemctl suspend
# Move your focus around
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
# Move the focused window with the same, but add Shift
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
# Switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10
# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10
# split the current object of your focus, horizontal and vertical
bindsym $mod+m splith
bindsym $mod+n splitv
# Switch the current container between different layout styles
bindsym $mod+ctrl+space layout toggle split
# Make the current focus fullscreen
bindsym $mod+f fullscreen
bindsym $mod+Shift+f fullscreen global
# Toggle the current focus between tiling and floating mode
bindsym $mod+Shift+space floating toggle
# Moving between workspaces
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev
# Switch workspaces between monitors
bindsym $mod+alt+Left move workspace to output left
bindsym $mod+alt+Right move workspace to output right
# Move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad
# Show the next scratchpad window or hide the focused scratchpad window.
bindsym $mod+minus scratchpad show
# SUPER +
bindsym $mod+g exec firefox
bindsym $mod+w exec brave --enable-features=UseOzonePlatform --ozone-platform=wayland
bindsym $mod+e exec vscodium
bindsym $mod+a exec thunderbird
bindsym $mod+s exec element-desktop --enable-features=UseOzonePlatform --ozone-platform=wayland
bindsym $mod+z exec thunar
bindsym $mod+t exec ferdium --enable-features=UseOzonePlatform --ozone-platform=wayland 
bindsym $mod+y exec freetube --enable-features=UseOzonePlatform --ozone-platform=wayland
bindsym $mod+p exec pamac-manager
# SUPER + Shift
bindsym $mod+shift+s exec signal-desktop --enable-features=UseOzonePlatform --ozone-platform=wayland
# OTHERS
bindsym Print exec grim ~/$(date '+%Y-%m-%d-%T')-screenshot.png
bindsym shift+Print exec grim -g "$(slurp)" ~/$(date '+%Y-%m-%d-%T')-screenshot.png
bindsym XF86AudioRaiseVolume exec pamixer -ui 5 && pamixer --get-volume > $WOBSOCK
bindsym XF86AudioLowerVolume exec pamixer -ud 5 && pamixer --get-volume > $WOBSOCK
bindsym XF86AudioMute exec pamixer --toggle-mute && ( pamixer --get-mute && echo 0 > $WOBSOCK ) || pamixer --get-volume > $WOBSOCK
bindsym XF86MonBrightnessUp exec brightnessctl set +10% | sed -En 's/.*\(([0-9]+)%\).*/\1/p' > $WOBSOCK
bindsym XF86MonBrightnessDown exec brightnessctl set 10%- | sed -En 's/.*\(([0-9]+)%\).*/\1/p' > $WOBSOCK
# Bind App to workspace
# assign [class="firefox"] $ws1
