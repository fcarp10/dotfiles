### Output configuration (swaymsg -t get_inputs)
input type:touchpad {
	accel_profile adaptive
	drag disabled
	# drag_lock enabled
	dwt enabled
	natural_scroll enabled
	pointer_accel 0.3
	scroll_factor 0.5
	scroll_method two_finger
	tap enabled
	middle_emulation disabled
}

input type:pointer {
	accel_profile adaptive
	drag enabled
	drag_lock enabled
	natural_scroll disabled
	pointer_accel -0.7
	scroll_factor 1.5
}

input type:keyboard {
    xkb_layout "us,es"
    xkb_options "grp:win_space_toggle"
	xkb_model "dell"
}